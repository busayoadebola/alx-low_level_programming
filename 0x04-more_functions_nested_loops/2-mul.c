#include "main.h"
#include <stdio.h>

/**
 * mul - to * two integers and displays the result
 * @a: to contain the first integer
 * @b: to contain the second integer
 * Return: 0 correct
 */

int mul(int a, int b)
{
	int mult;

	mult = a * b;
	return (mult);
}
