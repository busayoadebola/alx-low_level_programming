#include <stdio.h>

/**
 * main - A function to print the singl digits in base 10
 * Return: 0 (correct)
 */
int main(void)
{
	int n;

	for (n = 48; n < 58; n++)
		putchar(n);
	putchar('\n');
	return (0);
}
