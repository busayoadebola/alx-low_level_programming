#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/** main - A function to print the last number in n
 * Return: 0 (correctt)
 */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	printf("Last digit of" n "is" 
	return (0);
}
