#include <stdio.h>
#include <string.h>
/**
 * main - A program to print a string with the printf function
 * Return: 0 (correct)
 */
int main(void)
{
	char str1[] = "with proper grammar, but the outcome is a piece of art,";

	printf(str1);
	return (0);
}
