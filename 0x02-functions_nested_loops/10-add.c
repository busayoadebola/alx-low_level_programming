#include "main.h"
#include <stdio.h>

/**
 * add - to add two integers and displays the result
 * @a: to contain the first integer
 * @b: to contain the second integer
 * Return: 0 correct
 */

int add(int a, int b)
{
	int sum;

	sum = a + b;
	return (sum);
}
