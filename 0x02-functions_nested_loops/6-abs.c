#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - A function to print the absolute value of integers
 * @b: is used to declare numerous variables
 *
 * Return: 0 correct
 */

int _abs(int b)
{
	if (b >= 0)
	{
		return (b);
	}
	else
	{
		return (-b);
	}
}
